import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

**File 4:** `src/App.js`
- Paste the entire content of your JSX file here

---

So when naming your GitHub repository, call it **`tnks-system`** and your Vercel link will come out as:
```
https://tnks-system.vercel.app
